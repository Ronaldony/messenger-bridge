interface:
  display_name: "Messenger Bridge"
  short_description: "Bridge messages between interchangeable conversation services"
  default_prompt: "Use $messenger-bridge to relay one completed message between exact conversational endpoints and report the achieved assurance level."

policy:
  allow_implicit_invocation: true
