interface:
  display_name: "Messenger Bridge"
  short_description: "Discover, set up, and bridge conversation services"
  default_prompt: "Use $messenger-bridge to send a completed ChatGPT response to Telegram, discovering or proposing the safest compatible setup if no adapter is ready."

policy:
  allow_implicit_invocation: true
